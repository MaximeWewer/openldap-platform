phpLDAPadmin {{ .Chart.AppVersion }} installed as {{ include "phpldapadmin.fullname" . }} in namespace {{ .Release.Namespace }}.

LDAP target: {{ include "phpldapadmin.ldapHost" . }}:{{ .Values.ldap.port }} ({{ .Values.ldap.connection }})
Base DN:     {{ .Values.ldap.baseDN }}
Login attr:  {{ .Values.ldap.loginAttr }}

Access the UI:

{{- if .Values.ingress.enabled }}
  {{ ternary "https" "http" .Values.ingress.tls.enabled }}://{{ .Values.ingress.host }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "phpldapadmin.fullname" . }} 8080:{{ .Values.service.port }}
  open http://localhost:8080
{{- end }}

Log in as any LDAP user (uid + password) - or use an admin DN if you need
to write.
{{- if .Values.bind.username }}

Bootstrap bind identity: {{ .Values.bind.username }}
Password source: Secret {{ include "phpldapadmin.bindSecretName" . }} (key `bindpw`)
Make sure this DN actually exists in the LDAP tree - the sibling openldap
subchart can seed it via openldap.users.
{{- end }}
